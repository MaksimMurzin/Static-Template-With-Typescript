this is a template project with typescript instead of javascript

if you are making changes to typescript you will need to recompile using: 
    `npx tsc --watch`

now that everything is packaged with vite you can use:
` npm run dev ` 
to run the project
